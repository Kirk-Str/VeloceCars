using Microsoft.AspNetCore.Mvc;
using System;
using System.Collections.Generic;
using System.IO;
using System.Linq;
using System.Threading.Tasks;
using VeloceCars.Data;

namespace VeloceCars.Common
{
    public class Common
    {
        //private readonly ApplicationDbContext _context;

        //public Common(ApplicationDbContext context)
        //{
        //    _context = context;
        //}

        //public byte[] GetPackageImage(int id)
        //{
        //    var packageImage = _context.Package
        //        .Where(p => p.Id == id)
        //        .Select(p => p.Image)
        //        .SingleOrDefault();
            
        //    return packageImage;
        //}
    }
}
